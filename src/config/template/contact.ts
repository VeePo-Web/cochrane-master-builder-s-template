/**
 * Single source of truth for contact.
 * This site is email-only — no phone surfaces exist. Every Email link/label
 * on the site and in transactional emails reads from here.
 */
export const EMAIL = "inquiry@cochranemasterbuilders.com";

/** `mailto:` href for email links/buttons. */
export const EMAIL_HREF = `mailto:${EMAIL}`;
