## Goal

Embed the 8 uploaded `.docx` documents into the backend knowledge system as **immutable, byte-perfect source records**, paired with **separate partner interpretation documents** that tell the decision router how and when to consult each one. **Zero front-end changes.**

## State Note (important)

The previous summarized turns claimed source files were written, but the folder skeleton is empty on disk. I will create everything fresh from the uploads. To preserve verbatim text I will read each document's full saved tool-results file (some sections were truncated in the inline preview) before writing.

## The 8 Documents → Categories

| # | Upload | Category folder | Slug |
|---|---|---|---|
| 1 | General_Design_Prompt.docx | `experience-prompts/` | `master-design-persona-fantasy` |
| 2 | GENERAL_ANTI_GRAVITY_OPENING_PROMPT.docx | `experience-prompts/` | `anti-gravity-opening-engineer` |
| 3 | GENERAL_BRAND_IDENTITY_PROMPT.docx | `brand-identity/` | `brand-identity-architect` |
| 4 | General_CHRISTIAN_Colours_and_shapes_PROMPT.docx | `brand-identity/` | `colours-and-shapes-experience-philosophy` |
| 5 | General_FOOTER_Prompt-2.docx | `footer/` | `footer-architect` |
| 6 | General_1._OS_and_Backend-Processes_PROMPT.docx | `strategy/` (systems) | `os-systems-architect-copilot` |
| 7 | General_2._OS_and_Backend-Processes_AUDIT_SPECIALIST_PROMPT.docx | `strategy/` (systems) | `os-systems-audit-specialist` |
| 8 | General_3._OS_and_Backend-Processes_Mermaid_Diagram_Systems_Mapping_Mode_PROMPT.docx | `strategy/` (systems) | `os-mermaid-systems-mapping` |

## Files to Create (24 total)

### A) 8 Source documents (verbatim, immutable)

Path pattern: `src/master/knowledge/source-documents/<category>/<slug>.source.md`

Each starts with this banner only — body is the parsed document text, **unchanged**:

```
---
status: IMMUTABLE
do-not-edit: true
origin-file: <original .docx filename>
embedded-on: 2026-04-26
---
<!-- DO NOT EDIT THIS FILE. Source document preserved verbatim.
     Interpretation, adaptation, and brand-specific overrides
     belong in the matching .partner.md file. -->

[verbatim parsed text]
```

No edits, no removals, no "clean-up" — including any references the user mentioned (weddings, VeePo, Fantasy, etc.) stay exactly as written. Adaptation lives in the partner layer.

### B) 8 Partner documents (interpretation layer)

Path pattern: `src/master/knowledge/partner-documents/<category>/<slug>.partner.md`

Each follows this 12-section template:

1. Document title
2. Source reference (path to `.source.md`)
3. Document category & scope (global vs. specific)
4. Main purpose
5. What this document influences (UI surfaces, decisions, copy)
6. Trigger prompts / keywords that should consult it
7. How it should guide output quality
8. Relationship to brand identity / ICP / conversion
9. Conflict-resolution rules (general doc vs. brand-specific doc — brand wins)
10. Dependencies & related partner docs
11. Practical "when to use" examples
12. Anti-patterns this document protects against

### C) 1 New folder (already mostly exists)

Confirm `src/master/knowledge/source-documents/{experience-prompts,brand-identity,footer,strategy}/` and matching `partner-documents/` subfolders exist.

## Registry Updates

### `src/master/knowledge/decision-index.ts`

Add 8 new `DecisionRoute` entries with `brand: "general"` so the router can surface them. Each entry:

- `id`: e.g., `general-footer-architect`
- `category`: maps to folder (`brand-identity`, `experience-prompts`, `footer`, `systems`)
- `phrases`: trigger keywords (e.g., for footer doc: "footer", "site footer", "bottom of page", "footer IA", "fat footer", "sticky mini-footer")
- `partnerPath` / `sourcePath`: file paths
- `brand`: `"general"` (lower priority than brand-specific Cochrane Master Builders routes)

### `src/master/knowledge/decision-input.ts`

Extend hint maps so structured queries route correctly:

- `PAGE_SECTION_HINTS`: add `footer`, `hero`, `nav` cues
- `CONSTRAINT_HINTS`: add `mermaid`, `state-machine`, `audit`, `scale`, `accessibility`, `core-web-vitals`, `WCAG`, `liturgical`
- `CATEGORY_HINTS`: add `systems`, `brand-identity`, `experience`

### `src/master/knowledge/INDEX.md`

Add a new section: **"Cross-brand / General Intelligence Layer"** listing all 8 source+partner pairs, organized by category. Mark them explicitly as **secondary to brand-specific documents** (Cochrane Master Builders > general).

### `src/master/knowledge/README.md`

Append a short paragraph explaining the dual-layer system (source = immutable, partner = interpretation) and how the router consults general docs as a fallback when no brand-specific doc covers the topic.

### `src/master/knowledge/DECISION_ROUTER.md`

Add a worked example showing the router resolving a footer-related query: brand-specific footer rules first, then `footer-architect` general partner doc.

## Conflict Rule (Documented in Every Partner Doc)

- General docs define **craft baseline** (Fantasy.co-level standards).
- Brand-specific docs (Cochrane Master Builders) define **identity, palette, motifs, voice**.
- When they conflict, brand-specific wins on aesthetics; general wins on accessibility, performance, and structural integrity.

## Out of Scope (will not do)

- No front-end component edits.
- No changes to existing brand-specific documents.
- No editing of source-document content (verbatim rule).
- No edits to `src/integrations/supabase/*` or `.env`.
- Persona "remix" to brand-specific (you said this is a later step).

## Deliverable

24 new files + 5 edited registry/index files. Zero impact on the running site. The decision router will, after this change, be able to surface any of these 8 personas as a decision filter when relevant prompts arrive.
